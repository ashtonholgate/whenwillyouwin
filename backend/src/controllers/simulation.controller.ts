export const simulationController = () => {
  const startNewSimulation = (
  ) => {
    
  };
  
  return {
    startNewSimulation,
  };
};

export type SimulationController = ReturnType<typeof simulationController>;
